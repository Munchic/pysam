version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Installing pysam
          command: |
            echo Installing pysam
            python setup.py install --user
