version: 2
jobs:
  build:
    docker:
      - image: circleci/python:2.7.15
    steps:
      - checkout
      - run:
          name: Installing pysam
          command: |
            echo Installing pysam
            python setup.py install --user
